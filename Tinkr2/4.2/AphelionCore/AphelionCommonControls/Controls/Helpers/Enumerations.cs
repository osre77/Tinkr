using System;
using Microsoft.SPOT;

namespace Skewworks.NETMF.Controls
{
    public enum FileListMode
    {
        NameOnly = 0,
        NameSize = 1,
        NameSizeModified = 2,
    }

    public enum FileType
    {
        File = 0,
        Folder = 1,
    }
}
